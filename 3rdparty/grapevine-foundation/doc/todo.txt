TODO
- Camera + scene API (now weak pointers-> should be like Node, CameraComponent?)
- Setter naming in vector and matrix x() -> setX()
- Shader API for custom shaders to material
- Normal mapping to work with Tangent and Bitangent

FONT:
- 1 em == 1.0f size given to the font
- 1 em is distance between ascending and descending lines
- bounding box is given with the "shoulders" that go over ascending and descending
